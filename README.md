# meetingRoomDemo

## GitHub AI Code Review

This repository includes `.github/workflows/ai-review.yml` for automatic AI review on Pull Requests.

Required secret:
- `GEMINI_API_KEY`

Trigger:
- `pull_request` opened
- `pull_request` reopened
- `pull_request` synchronize